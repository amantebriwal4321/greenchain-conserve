# greenchain-team
GreenChain is an IoT-based system that tracks waste disposal using a smart bin.
It measures waste weight using sensors, sends the data to a backend server, stores it in a database, and displays it on an admin dashboard.
